apiVersion: batch.backupscheduler.io/v1
kind: CronJob
metadata:
  labels:
    app.kubernetes.io/name: backup-scheduler
    app.kubernetes.io/managed-by: kustomize
  name: cronjob-sample
spec:
  # TODO(user): Add fields here
